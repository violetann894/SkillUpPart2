public class Pikachu extends Pokemon{

    //Subclass, inherits the public interface

    //Default Pikachu
    public Pikachu(){
        setHp(35);
        setAttack(55);
        setDefense(30);
        setSpeed(90);
    }

    //We could make another constructors, one with all the parameters

    //Set of getters and setters

}
